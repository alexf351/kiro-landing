# Copy to .env (ignored by git and by Vercel) or export in your shell.
# Used only by blog-engine/tools/gen-hero-images.mjs at build time on your machine.
# Get a key at https://aistudio.google.com/apikey — never commit the real value.
GEMINI_API_KEY=

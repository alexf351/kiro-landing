{
  "name": "iro-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Static marketing site for Iro AI. This package.json exists only so Vercel can install the one dependency that middleware.ts needs (AI crawler hits reported to PostHog) \u2014 there is deliberately no build script, because the site is prebuilt HTML committed to the repo and served from the repo root.",
  "dependencies": {
    "@vercel/functions": "3.9.5"
  }
}
